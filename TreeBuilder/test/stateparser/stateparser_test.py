import unittest



class Test_StateParser(unittest.TestCase):

    def test_process_token_with_no_states(self):
        # token_mock = Mock(Token)
        # token_mock.kind = Mock()
        # token_mock.kind.assert_not_called()
        # token_stream_mock = Mock(TokenStream)
        # s = StateParser(token_stream_mock)
        #
        # self.assertIsNone(s.process_token(token_mock))
        self.assertTrue(True)

    def test_process_token_with_states_with_matching_token_kind(self):
        # state_mock = Mock()
        self.assertTrue(True)